# AGENTS.md

- Refactor freely. Prefer clean, maintainable code over backward compatibility — breaking changes to CLI commands, flags, JSON output shapes, and the rd-* schema are allowed when they improve the design.
- Each skill is self-contained under its own directory. Treat that skill's `SKILL.md` as the canonical reference; do not duplicate per-skill catalogs, command tables, or architecture notes into this file.
