{
	"name": "richdoc",
	"version": "0.2.0",
	"private": true,
	"type": "module",
	"description": "Framework for AI-authored HTML documents",
	"scripts": {
		"build": "bun run build.ts",
		"build:dev": "bun run build.ts --dev",
		"lint": "biome check .",
		"lint:fix": "biome check --fix .",
		"format": "biome format --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@types/bun": "latest",
		"lucide-static": "1.16.0",
		"typescript": "^5.6.3"
	}
}
