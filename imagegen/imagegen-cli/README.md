# imagegen-cli

Agent-facing CLI for generating and editing images via Google Gemini.
See `../SKILL.md` for the skill contract and usage examples.

Requires `uv` ([install](https://docs.astral.sh/uv/)). The first call provisions
the Python environment automatically.
